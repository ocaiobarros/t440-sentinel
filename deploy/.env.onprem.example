# ╔══════════════════════════════════════════════════════════════════╗
# ║  FLOWPULSE ON-PREMISE — Configuração de Ambiente               ║
# ║  Copie para .env e ajuste os valores antes de iniciar.          ║
# ║  © 2026 CBLabs                                                  ║
# ╚══════════════════════════════════════════════════════════════════╝

# ─── API / Frontend ───────────────────────────────────────
# O frontend usa estas variáveis em tempo de BUILD (Vite).
# Para on-prem, apontar para o próprio servidor local.
VITE_SUPABASE_URL=http://localhost:3060
VITE_SUPABASE_PUBLISHABLE_KEY=flowpulse-onprem-anon-key

# ─── Servidor Express ─────────────────────────────────────
PORT=3060
STATIC_DIR=/usr/share/flowpulse/web
STORAGE_DIR=/var/lib/flowpulse/data

# ─── PostgreSQL Local ─────────────────────────────────────
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=flowpulsedb
DB_USER=flowpulse
DB_PASS=__GENERATE__

# ─── Segurança ────────────────────────────────────────────
JWT_SECRET=__GENERATE__
JWT_EXPIRY=24h
ZABBIX_ENCRYPTION_KEY=__GENERATE__

# ─── Controle de Migrations ──────────────────────────────
# 0 = não aplica schema automaticamente (padrão — recomendado)
# 1 = aplica schema.sql no postinst
AUTO_MIGRATE=0
